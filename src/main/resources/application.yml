---
server:
  port: 9091

logging:
  level:
    org.springframework.web: ERROR
    vn.com: DEBUG
  file: /opt/blogs-of-trung-bro/log/bo-server.log

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: blogsTrungBro

jwt:
  header: Authorization
